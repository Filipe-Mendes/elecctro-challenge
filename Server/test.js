import authenticationHandlers from "./server/api/handlers/authenticationHandlers.js";
import authenticationAccessDb from "./server/data-access/authenticationAccessDb.js";
authenticationHandlers
// const a = await authenticationAccessDb.createUser({user_name: 'd', name:'a',email:'b',password:'c'})

// console.log(
authenticationHandlers.postLogin(null,null);